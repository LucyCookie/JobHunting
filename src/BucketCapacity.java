import java.util.Arrays;
import java.util.HashSet;
import java.util.Set;

/**
 * Created by qiqu on 2/21/16.
 */
public class BucketCapacity {
    public int steps(int[] buckets, int target){
        int[] stat=new int[buckets.length];
        Arrays.sort(buckets);
        Set<String> dejavu=new HashSet<>();
        int step=0;
        while (true){

        }
    }
}
